Use these 4 animals: **crow, beaver, octopus, elephant**.

Use these 4 tools: **bucket, rope, mirror, lantern**.

Generate exactly 16 Python functions, one for every animal/tool pair.

Each function name must be `animal_tool`.

Each function body must contain exactly one `print()` statement that outputs a unique, plausible one-sentence scenario where that animal uses that tool to solve a problem.  For "plausible", you can assume the tool is built to fit the animal, otherwise be strict about plausibility.

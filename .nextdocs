# .nextdocs

Quick retrieval notes for modern React patterns.

## React examples

### 1) Simple functional component
```jsx
export function Greeting({ name = 'friend' }) {
  return <h1>Hello, {name}!</h1>;
}
```

### 2) Local state with `useState`
```jsx
import { useState } from 'react';

export function Counter() {
  const [count, setCount] = useState(0);

  return (
    <button onClick={() => setCount((c) => c + 1)}>
      Count: {count}
    </button>
  );
}
```

### 3) Data fetch with `useEffect`
```jsx
import { useEffect, useState } from 'react';

export function UsersList() {
  const [users, setUsers] = useState([]);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    let ignore = false;

    async function loadUsers() {
      const response = await fetch('/api/users');
      const data = await response.json();
      if (!ignore) {
        setUsers(data);
        setLoading(false);
      }
    }

    loadUsers();
    return () => {
      ignore = true;
    };
  }, []);

  if (loading) return <p>Loading...</p>;
  return <ul>{users.map((u) => <li key={u.id}>{u.name}</li>)}</ul>;
}
```

### 4) Memoized computed data with `useMemo`
```jsx
import { useMemo } from 'react';

export function FilteredItems({ items, query }) {
  const filtered = useMemo(() => {
    const q = query.trim().toLowerCase();
    return items.filter((item) => item.name.toLowerCase().includes(q));
  }, [items, query]);

  return <pre>{JSON.stringify(filtered, null, 2)}</pre>;
}
```

### 5) Controlled form input
```jsx
import { useState } from 'react';

export function EmailForm() {
  const [email, setEmail] = useState('');

  function handleSubmit(event) {
    event.preventDefault();
    console.log('Submitted:', email);
  }

  return (
    <form onSubmit={handleSubmit}>
      <input
        type="email"
        value={email}
        onChange={(e) => setEmail(e.target.value)}
        placeholder="you@example.com"
      />
      <button type="submit">Save</button>
    </form>
  );
}
```

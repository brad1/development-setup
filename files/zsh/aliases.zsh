# Basic aliases for interactive use.
# Most former entries are now available as `navi` cheat sheets under
# `$DEVSETUP/files/custom/**`.

alias ll="ls -lv --group-directories-first"
alias la="ls -A"
alias l="ls -CF"

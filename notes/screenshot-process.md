# Screenshot capture notes

The resource monitor screenshot was captured by running the Vite dev server for the React dashboard and using the browser tools Playwright runner with Firefox to open `http://127.0.0.1:4173/` and take a full-page screenshot saved to `artifacts/react-dashboard-resource-monitor.png`.
